package zuper.dev.android.dashboard.navigation

object Screens {
    const val DASHBOARD = "dashboard"
    const val JOB= "job/{jobData}"
}

object Arguments {
    const val JOB_DATA = "jobData"
}

object PlaceHolder {
    const val JOB_DATA = "{jobData}"
}